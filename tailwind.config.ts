/**
 * Tailwind v4 config.
 *
 * NOTE: Tailwind v4 is CSS-first — brand tokens and utilities are declared via
 * @theme in src/styles/globals.css. This file is kept for tooling compatibility
 * (e.g. IDE IntelliSense, future codemod tooling).
 *
 * Content scanning is handled via `@source` in globals.css.
 */
export default {}
